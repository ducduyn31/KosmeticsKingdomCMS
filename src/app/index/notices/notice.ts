export class Notice {
  constructor(public _id: string, public subject: string, public author: string, public rawContent: string, public __v: number, public view: number, public date: string) {
  }
}
